<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!-- Debug-only: lets adb force a Sense evaluation tick.
             Exported so `adb shell am broadcast` can reach it; this manifest
             is merged into DEBUG builds exclusively. -->
        <receiver
            android:name=".SenseDebugReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.reset.app.DEBUG_SENSE_TICK" />
            </intent-filter>
        </receiver>

        <!-- Debug-only: the Sense pipeline inspector. Appears as its own
             "Sense Debug" launcher icon on debug installs, and is reachable
             via `adb shell am start -n com.reset.app/.SenseDebugActivity`.
             TO REMOVE: delete this entry + SenseDebugActivity.kt. -->
        <activity
            android:name=".SenseDebugActivity"
            android:label="Sense Debug"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
