package com.reset.feature.home.ui

import androidx.compose.ui.graphics.Color

/**
 * Night check-in card palette — this screen's post-quiet-hours theme (design 1c). Colours
 * live here (not the shared [com.reset.core.designsystem.AppColors]) because they are
 * specific to this one screen's night variant.
 */
object HomeTone {
    val nightBackground = Color(0xFF123543)
    val nightCard = Color(0xFF1C4A5C)
    val nightRow = Color(0xFF16404F)
    val nightInk = Color(0xFFF2EEE3)
    val nightInkMuted = Color(0xFF7FA3AE)
    val nightInkFaint = Color(0xFF5E828D)
    val nightAccent = Color(0xFFE8C289)
    val nightCta = Color(0xFF2E7C8C)
}
