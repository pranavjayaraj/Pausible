<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Usage access is a special app-op the user grants in Settings; the
         host app deep-links there via SensePermissions guidance. -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Physical context (still / on-foot / in-vehicle) for gating. -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission
        android:name="com.google.android.gms.permission.ACTIVITY_RECOGNITION"
        android:maxSdkVersion="28" />

    <application>
        <receiver
            android:name=".activity.ActivityTransitionReceiver"
            android:exported="false" />
    </application>

</manifest>
